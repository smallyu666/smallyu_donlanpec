# DongLanpec 项目交接说明

> 适用范围：项目管理、条件输入、总界面 UI  
> 代码路径：`DongLanpec-localNEN最新版`（本地正常运行备份）  
> 文档日期：2026-08

---

## 一、项目是什么

**DongLanpec** 是一套基于 **PyQt5** 的桌面端压力容器/换热器设计软件。用户按固定流程完成：建项目 → 定义产品 → 输入设计条件 → 定义元件/管口 → 布管 → 强度计算 → 出图/出报告。

### 技术栈速览

| 层次 | 技术 |
|------|------|
| UI | PyQt5 + Qt Designer（`.ui` 文件）+ QSS 样式 |
| 语言 | Python 3 |
| 数据库 | MySQL（多库：项目需求库、产品需求库、产品设计活动库、产品条件库等） |
| 本地文件 | 每个产品有独立文件夹：`pro_id.csv`、`条件输入数据表.xlsx`、`管口导入模板.xlsx` |
| 计算 | 外部 .NET DLL，通过 JSON 桥接 |
| 2D | AutoCAD 集成 |

**启动入口：** `main.py` → 加载 `main_viewer333_new.ui` 作为总壳层。

---

## 二、负责模块 & 关键文件

### 1. 总界面 UI（壳层）

| 文件 | 作用 |
|------|------|
| `main.py` | 程序入口、模块导航、Tab 管理、底部状态栏 `line_tip` |
| `main_viewer333_new.ui` | 顶部导航按钮、中央 Tab 区、底部提示栏 |
| `register.py` / `login.ui` | 登录注册 |

导航按钮与模块的对应关系（`main.py`）：

| 按钮 ID | Tab 标题 | 模块入口 |
|---------|----------|----------|
| `btn_project` | 项目管理 | `modules/chanpinguanli/main2.py` → `cpgl_Stats` |
| `btn_condition` | 条件输入 | `modules/condition_input/view.py` → `DesignConditionInputViewer` |
| `btn_material` | 元件定义 | `modules/cailiaodingyi/paradefine_view.py` |
| `btn_pipe` | 管口及附件定义 | `modules/guankoudingyi/dynamically_adjust_ui.py` → `Stats` |
| `btn_pipeDesign` | 管束设计 | `modules/buguan/buguan_ziyong/My_Piping.py` → `TubeLayoutEditor` |
| `btn_cal` | 设计运算 | `modules/qiangdujisuan/jiekou_python/jisuanjiemian.py` |
| `btn_2D` | 图纸绘制 | `modules/TwoD/TwoD_tab.py` |
| `btn_docs` | 文本说明生成 | `modules/wenbenshengcheng/wenbenshengcheng.py` |
| `btn_config` | 预定义 | `modules/yudingyi/predefined.py` |
| `btn_3D` | 模型创建 | `modules/Three3D/Three3D.py`（占位） |

**设计模式：** `.ui` 负责布局，Python 负责逻辑；各模块通过 `line_tip` 向底部状态栏发提示。

---

### 2. 项目管理（`modules/chanpinguanli/`）

| 文件 | 作用 |
|------|------|
| `main2.py` | 项目管理页入口，加载 `guanli_new.ui` |
| `chanpinguanli_main.py` | 核心业务（~3400 行）：产品定义、保存、复制、容器开发中判断 |
| `bianl.py` | **全局状态**（当前项目 ID、产品 ID、各控件引用、模式） |
| `common_usage.py` | 数据库连接、产品类型映射 |
| `local_product_folder.py` | 本地三文件校验与恢复 |
| `new_project_button.py` / `open_project.py` / `modify_project.py` | 新建/打开/修改项目 |
| `ui_style.py` | 项目管理统一按钮/弹窗样式 |
| `功能.txt` | 已知 UX 问题清单（部分已解决） |

**产品类型（来自数据库 `产品类型型式表`）：**

- **管壳式热交换器**：AEU、BEU、AES、BES、NEN、BEM、AEM、AKU、BKU、NEN(Head) 等
- **卧式容器**：单腔型 ✅ / 双腔型 ❌（开发中）
- **立式容器**：全部 ❌（开发中）

**容器开发中判断逻辑**（`chanpinguanli_main.py`）：

```python
def _is_container_definition_developing(product_type: str, product_form: str = "") -> bool:
    """立式容器整类开发中；卧式容器仅双腔型开发中。"""
    if product_type == "立式容器":
        return True
    if product_type == "卧式容器" and product_form == "双腔型":
        return True
    return False
```

**本地产品文件夹三文件：**

| 文件 | 换热器 | 容器 |
|------|--------|------|
| 产品 ID | `pro_id.csv` | `pro_id.csv` |
| 条件输入 | `条件输入数据表.xlsx` | `容器条件输入数据表.xlsx` |
| 管口模板 | `管口导入模板.xlsx` | `容器管口导入模板.xlsx` |

模板文件位于 `modules/chanpinguanli/` 目录下。

---

### 3. 条件输入（`modules/condition_input/`）

| 文件 | 作用 |
|------|------|
| `view.py` | 主界面（~2200 行），五张表、保存/校验流程 |
| `viewer_new.ui` | 条件输入布局 |
| `funcs/funcs_cdt_input.py` | 核心逻辑（~7000 行）：加载/保存、表格渲染、容器/换热器分支 |
| `funcs/funcs_def_check.py` | 字段校验（DN、压力、温度、GB/T 150/151） |
| `funcs/multi_conditions_dialog.py` | 多工况对话框 |
| `funcs/design_data_delegate.py` | 表格单元格委托 |
| `funcs/ctrl_helper.py` | 撤销/重做 |

**五张表：**

| 表名 | 含义 | 容器差异 |
|------|------|----------|
| `tableWidget_product_std` | 产品标准 | 共用 |
| `tableWidget_design_data` | 设计数据 | 容器：单层表头；换热器：双层表头 |
| `tableWidget_general_data` | 通用数据 | **容器隐藏此 Tab** |
| `tableWidget_trail_data` | 无损检测 | 列宽比例不同 |
| `tableWidget_coating_data` | 涂漆 | 共用 |

**容器 vs 换热器分支入口：** `is_container_viewer(viewer)` 在 `funcs/funcs_cdt_input.py` 中，多处逻辑据此分支。

---

## 三、数据流

```
项目管理 → 条件输入 → 元件定义 → 管口定义 → 管束设计 → 设计运算 → 图纸 → 文本生成 → 预定义
```

详细血缘文档在 `_doc_flow_extract/`：

| 文件 | 内容 |
|------|------|
| `sheet0_界面总览.txt` | 各模块读写哪些库表 |
| `filter_my_modules/界面血缘_我的模块.txt` | 项目管理 + 条件输入专用 |
| `filter_my_modules/字段值流转_我的模块.txt` | 字段级流转 |
| `filter_my_modules/字段级血缘_我的模块.txt` | 字段级血缘 |

### 核心规律

1. 界面数据 ↔ MySQL `产品设计活动库.产品设计活动表_*`
2. 同时同步到本地 Excel（`条件输入数据表.xlsx`）
3. 下游模块只读活动库，不直接读 Excel
4. 产品 ID 是贯穿全链路的 key；切换产品通过 `product_manager.product_id_changed` 信号广播

### 各模块数据读写概要

| 序号 | 模块 | 主要读取 | 主要写入 | 本地产出 |
|------|------|----------|----------|----------|
| 1 | 项目管理 | 项目需求库、产品需求库、产品定义库 | 项目需求表、产品需求表、产品设计活动表 | 项目目录、`pro_id.csv`、条件/管口 Excel 模板 |
| 2 | 条件输入 | 产品条件库（模板/配置）、产品设计活动表 | 产品设计活动表_设计数据表 | 条件输入数据表.xlsx |
| 3 | 元件定义 | 产品设计活动表、材料库 | 产品设计活动表_元件* | — |
| 4 | 管口定义 | 产品设计活动表 | 产品设计活动表_管口* | 管口 Excel 导入/导出 |
| 5 | 管束设计 | 产品设计活动表、布管参数 | 产品设计活动表_布管* | — |
| 6 | 设计运算 | 产品设计活动表 | 计算结果表、日志表 | `shuru_jisuan.json` → DLL → `jisuan_output_new.json` |
| 7 | 图纸绘制 | 产品设计活动表、计算结果 | — | AutoCAD DWG |
| 8 | 文本生成 | 产品设计活动表、计算结果 | — | 计算书 DOCX、材料清单等 |
| 9 | 预定义配置 | 配置库 | 配置库 user_config 等 | — |

---

## 四、当前开发状态

| 产品线 | 状态 | 工作重点 |
|--------|------|----------|
| **管壳式热交换器** | ✅ 全流程完成 | 维护、修 Bug |
| **卧式容器·单腔型** | ⚠️ 部分完成 | 参数增删改、约束调整、补缺失功能 |
| **卧式容器·双腔型** | ❌ 产品定义被拦截 | 暂不动 |
| **立式容器** | ❌ 产品定义被拦截 | 后续主力开发方向 |
| **3D 模型创建** | ❌ 占位页 | 可改 UI，无后端 |

### 各模块完成度矩阵

| 模块 | 换热器 | 卧式·单腔 | 卧式·双腔 | 立式 |
|------|--------|-----------|-----------|------|
| 产品定义 | ✅ | ✅ | ❌ | ❌ |
| 条件输入 | ✅ | ✅ | — | — |
| 元件定义 | ✅ | ⚠️ 部分 | — | — |
| 管口定义 | ✅ | ⚠️ 部分 | — | — |
| 管束设计 | ✅ | N/A（隐藏） | — | — |
| 设计运算 | ✅ | ❓ 待确认 | — | — |
| 图纸绘制 | ✅ | ⚠️ 部分 | — | — |
| 文本生成 | ✅ | ❓ 待确认 | — | — |
| 3D 模型 | ❌ | ❌ | ❌ | ❌ |

### 卧式单腔型已知缺口

- **管口 Excel 导出**未配置：`modules/guankoudingyi/funcs/funcs_pipe_data_in_out.py` 对容器产品会抛出 `RuntimeError("容器产品管口导出模板尚未配置...")`
- **设计运算 / 二维图纸 / 文本生成**：大概率仍以换热器逻辑为主，容器适配待确认
- **管束设计**：容器产品通过 `tube_bundle_toolbar.py` 隐藏该模块入口

### 目录结构概览

```
项目根目录/
├── main.py                         # 程序入口
├── main_viewer333_new.ui           # 总界面 UI
├── register.py / login.ui          # 登录
├── config.ini                      # 登录配置
├── help.html                       # 内置帮助
├── README.md                       # 产品复制功能说明
├── _doc_flow_extract/              # 界面/数据流文档
├── icons/
├── logs/
└── modules/
    ├── chanpinguanli/              # 项目管理 ★
    ├── condition_input/            # 条件输入 ★
    ├── cailiaodingyi/              # 元件定义
    ├── guankoudingyi/              # 管口及附件定义
    ├── buguan/                     # 管束设计
    ├── qiangdujisuan/              # 设计运算
    ├── TwoD/                       # 图纸绘制
    ├── wenbenshengcheng/           # 文本说明生成
    ├── yudingyi/                   # 预定义配置
    └── Three3D/                    # 模型创建（占位）
```

（★ 为本文档重点覆盖模块）

---

## 七、调试技巧 & 注意事项

1. **`bianl.py` 是全局状态中心** — 改项目管理前先弄清 `current_project_id`、`product_id`、`product_mode` 等变量的含义和赋值时机。

2. **改 `.ui` 文件** — 用 Qt Designer 或文本编辑；改完需重启程序，`uic.loadUi` 不会热更新。

3. **大文件先搜索再局部改** — 避免通读：
   - `funcs_cdt_input.py`（~7000 行）
   - `chanpinguanli_main.py`（~3400 行）
   - `My_Piping.py`（~48000 行，管束设计，非本模块职责）

4. **数据库改动谨慎** — 参数模板多在 `产品条件库`，活动数据在 `产品设计活动库`；改表结构前先备份。

5. **容器分支影响面广** — 搜索 `is_container`、`容器` 查看所有相关分支，避免只改一处导致行为不一致。

6. **已知内部待办** — `modules/chanpinguanli/功能.txt` 记录了 UX 层面的已知问题，其中部分已标注「解决」，可作参考。

7. **网络/数据库** — `main.py` 第 89 行 `check_server_domain()` 已注释，本地开发可直连 localhost MySQL；生产环境需确认连接配置（`common_usage.py`、`db_cnt.py` 等）。

8. **产品切换信号** — 条件输入通过 `product_manager.product_id_changed.connect(on_product_id_changed)` 监听产品切换，改项目管理保存逻辑时注意信号是否正常触发。
